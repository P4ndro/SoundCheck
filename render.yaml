services:
  - type: web
    name: soundcheck-api
    runtime: node
    rootDir: apps/api
    buildCommand: npm ci --include=dev && npm run build:deploy
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
